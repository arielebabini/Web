# Test Environment Configuration
NODE_ENV=test

# Test Database Configuration - Using Docker (credenziali corrette)
TEST_DB_HOST=localhost
TEST_DB_PORT=5432
TEST_DB_NAME=coworkspace_test
TEST_DB_USER=coworkspace_user
TEST_DB_PASSWORD=postgress

# Same as production for consistency
DB_HOST=localhost
DB_PORT=5432
DB_NAME=coworkspace_test
DB_USER=coworkspace_user
DB_PASSWORD=postgress

# JWT Configuration (same as dev)
JWT_SECRET=your_super_secret_jwt_key_change_in_production
JWT_EXPIRES_IN=7d

# Disable external services in tests
ENABLE_SWAGGER=false
EMAIL_USER=test@example.com
EMAIL_PASS=test_password
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_REDIRECT_URI=
LOG_LEVEL=error
REDIS_URL=redis://localhost:6379
